apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend-app
  #namespace: argocd
  namespace: openshift-gitops 
spec:
  project: default
  source:
    repoURL: 'https://github.com/k8strainer/argo-app-of-app.git'
    targetRevision: main
    path: child-apps/backend
    directory:
      recurse: true
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: app-of-app-backend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
